# CTO 보고 — Hermes AI Worker 검증 & 중앙 Drive 인증 브릿지 구축

**날짜:** 2026-07-14
**작성자:** AIR Studio 개발 (Claude Code 세션)

---

## 요약 (Executive Summary)

1. **Hermes AI Worker(주제 자동 리서치) — 로컬 실행 검증 완료.** 실제 Gemini API로 주제
   생성까지 end-to-end 확인. 단, **아직 main/프로덕션에 병합되지 않은 상태**이며 정식
   배포(설치 프로그램 배포)는 하지 않았다.
2. **구글 드라이브 접근 방식을 중앙 서버 브릿지로 전환 — 완료, 프로덕션 배포 및 실제
   호출 검증까지 마침.** 렌더링 파이프라인이 쓰던 로컬 `token.pickle` 파일을 어떤
   PC에도 두지 않도록 구조를 바꿨다.

---

## 1. Hermes AI Worker — "돌아가는가?" 검증

### 배경
유저가 렌더링 버튼을 누르면 영상 에셋이 구글 드라이브로 올라가고, 렌더링 PC의
프로그램이 그걸 받아 렌더링 후 다시 업로드하는 파이프라인에 **Hermes(주제 자동
리서치 AI)를 같은 실행파일에 통합**하는 작업이 이전에 다른 방향(중앙 서버 API
설계)으로 새어나갔던 것을 원래 목적("실행 가능한 단일 exe")으로 되돌리는 작업이었다.

### 검증한 것
- 통합 실행파일 `AIRWorker.exe` 1개를 그냥 실행 → Manager가 자동으로 4개 프로세스
  기동: **Render Worker, Hermes Worker(진짜 AI 호출), Local API, (업데이터는 대기)**.
  사람이 여러 프로그램을 따로 켤 필요 없음.
- Local API(`127.0.0.1:8765`)로 실시간 상태 조회 → 전부 정상 heartbeat 확인.
- 실제 키워드("건강한 식습관")로 주제 리서치 작업을 넣어봄 → Hermes가 실제 Gemini
  API를 호출해 15초 만에 유튜브 롱폼 주제 3건(제목/요약/근거)을 실제로 생성.
- API 키가 없을 때의 실패 처리도 확인 — 3회 재시도 후 명확한 에러로 깔끔하게 종료
  (크래시 없음, 키 값 로그 유출 없음).
- 렌더링 작업과 Hermes 작업이 동시에 있을 때 렌더링이 우선(Hermes 작업은 대기)되는
  것도 이전 세션에서 확인됨.

### 현재 상태
- 코드는 GitHub PR #86(`feat/air-0227e-p3-real-hermes-worker`)에 있고 **아직 OPEN**
  (main 미병합). PR #82~#86 전부 병합 안 된 상태.
- 코드사이닝 인증서 없음, 완전히 깨끗한 Windows PC/VM에서의 최종 확인 안 됨 —
  **정식 배포(설치 프로그램으로 사내/외부 배포) 전 단계**.
- 결론: **기능적으로는 잘 작동함을 실제로 확인했다. 배포 여부/시점은 별도 결정
  필요.**

---

## 2. 중앙 Google Drive 인증 브릿지

### 배경
유저앱(영상 생성 데스크톱 앱)이 소수 직원이 아니라 **불특정 다수(수백 대 규모)**에
배포될 계획으로 바뀌면서, 구글 드라이브 접근에 쓰던 `token.pickle`(만료 없이 재사용
가능한 강력한 인증 파일)을 각 설치본에 두는 방식이 과거 `SUPABASE_SERVICE_ROLE_KEY`
유출 사고(AIR-0225B)와 같은 유형의 위험으로 식별됨.

### 변경 내용
- 신규 서버 엔드포인트(`auth-web/api/desktop-drive-token`)가 구글 인증 정보를
  **서버에만** 보관하고, 요청마다 ~1시간짜리 제한된 접근 토큰만 발급.
- 실제 대용량 파일(렌더링된 영상)은 서버를 거치지 않고 클라이언트↔구글이 직접
  주고받음 — 서버 부하/제한과 무관.
- 유저앱은 기존 로그인 세션으로, 렌더링 PC는 이미 갖고 있던 자격증명을 실시간
  검증하는 방식으로 인증.
- `services/google_drive_service.py` 한 곳만 수정 — 업로드/다운로드 로직 자체는
  그대로.

### 검증
- PR #89, #90 모두 **main 병합 + 프로덕션 배포 완료**.
- 실제 프로덕션 서버에 호출해서 진짜 접근 토큰 발급 확인 → 그 토큰으로 실제
  구글 드라이브 API 호출까지 성공(기존 렌더링 산출물 파일들 정상 조회).
- 잘못된 인증 정보는 정상적으로 거부됨(401) 확인.

### 남은 것
- PR #88(에셋 업로드 시 카테고리 폴더 정리)은 이 브릿지와 자동으로 호환되며,
  병합 여부만 결정하면 됨.

---

## 다음 결정이 필요한 사항

1. Hermes Worker(PR #82~#86)를 main에 병합하고 정식 배포할지, 시점은 언제로 할지.
2. PR #88 병합 여부.
3. (기존 이관 항목) `SUPABASE_SERVICE_ROLE_KEY` 신규 키 발급 및 교체 — 아직 미착수.
