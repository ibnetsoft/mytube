# AIR-0144: admin-publish-request API 이관 완료 보고서

Date: 2026-07-05
Branch: feature/AIR-0144-admin-publish-request-extraction
Status: COMPLETE

---

## 1. 개요
`main.py`에 고유 잔류하던 `POST /api/projects/{project_id}/admin-publish-request` (`request_admin_publish`) API 및 해당 엔드포인트 전용 헬퍼 함수인 `_resolve_local_output_asset_path()`를 `app/routers/youtube.py`로 성공적으로 이관 완료하였습니다.

---

## 2. 구현 결과

### 1) 이관된 API
- `POST /api/projects/{project_id}/admin-publish-request`

### 2) 이관된 Helper
- `_resolve_local_output_asset_path()`

### 3) main.py 변경 내역
- `request_admin_publish` 라우트 핸들러 물리적 삭제 완료.
- `_resolve_local_output_asset_path()` 헬퍼 함수 물리적 삭제 완료.
- 다른 기능에 영향을 미칠 수 있는 미사용/dead import는 존재하지 않아 (BackgroundTasks 등은 다른 API에서 사용 중) 그대로 보존 완료.

### 4) app/routers/youtube.py 변경 내역
- FastAPI `BackgroundTasks` 및 `drive_bundle_service` 관련 임포트 보충 완료.
- 파일 하단에 `request_admin_publish` API 추가 완료.
- `_resolve_local_output_asset_path()` 는 기존 AIR-0143 단계에서 이미 이식 완료되어 있었으므로, 추가 변경 없이 해당 헬퍼를 정상 사용하도록 구성.

### 5) 삭제된 main.py 코드 라인 수
- `main.py` 기준 총 **71라인** 순삭제 완료.

---

## 3. 검증 및 테스트 결과
1. **정적 검사 (AST)**: `python -m py_compile main.py` 실행 완료 $\rightarrow$ **오류 없음 (PASS)**
2. **런타임 임포트 검증**: `uv run python -c "import main"` 실행 $\rightarrow$ **정상 기동 완료 (PASS)**
3. **API URL 및 동작성**: APIRouter prefix와 결합하여 기존과 100% 동일한 URL 패턴 `/api/projects/{project_id}/admin-publish-request`로 라우팅됨을 확인 (PASS).
4. **회귀 테스트**: `uv run pytest tests` 실행 결과 정상 구동 (PASS).
