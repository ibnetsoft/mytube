# AIR-0143: YouTube/Publish 라우터 추출 완료 보고서

Date: 2026-07-05
Branch: feature/AIR-0143-youtube-publish-router-extraction
Status: COMPLETE

---

## 1. 개요
`main.py` 내부의 YouTube 관련 API 7개 라우트와 3개 유틸 함수, Pydantic `SearchRequest` 모델을 신설된 `app/routers/youtube.py` 라우터로 이관 완료하였습니다.
`POST /api/projects/{project_id}/admin-publish-request` API는 CTO 지시에 따라 이관하지 않고 `main.py`에 원본 그대로 유지하였습니다.

---

## 2. 구현 결과

### 1) 신규 생성 파일
- [app/routers/youtube.py](file:///c:/Projects/AIR-Studio/app/routers/youtube.py)

### 2) 이동된 API 목록 (7개)
1. `POST /api/youtube/search` (`youtube_search`)
2. `POST /api/projects/{project_id}/youtube/auto-upload` (`auto_upload_youtube`)
3. `POST /api/projects/{project_id}/youtube/public` (`publicize_youtube_video`)
4. `GET /api/youtube/videos/{video_id}` (`youtube_video_detail`)
5. `GET /api/youtube/comments/{video_id}` (`youtube_comments`)
6. `GET /api/youtube/channel/{channel_id}` (`youtube_channel`)
7. `POST /api/youtube/upload-external/{project_id}` (`upload_external_to_youtube`)

### 3) 이동된 Helper 및 모델 목록
- Pydantic 모델 `SearchRequest`
- `_resolve_local_output_asset_path()` (※ `request_admin_publish` 지원을 위해 `main.py`에도 독립적으로 유지)
- `_resolve_youtube_token_path()`
- `_resolve_project_thumbnail_path()`

### 4) main.py 변경 내역
- `youtube_router` 임포트 및 `app.include_router(youtube_router.router)` 추가 완료.
- 7개 라우트 및 유틸리티 함수 2개, Pydantic 모델 1개 물리적 삭제 완료.

### 5) 삭제된 main.py 코드 라인 수
- `main.py` 기준 총 **485라인** 순삭제 완료.

---

## 3. 검증 및 테스트 결과
1. **정적 검사 (AST)**: `python -m py_compile main.py` 실행 완료 $\rightarrow$ **오류 없음 (PASS)**
2. **런타임 임포트 검증**: `uv run python -c "import main"` 실행을 통해 모듈 바인딩 정상 기동 확인 (PASS).
3. **API URL 일치 검사**: `app/routers/youtube.py` 에 prefix `/api`가 정상 적용되어 기존 API들과 100% 동일한 경로 패턴 유지 확인 (PASS).
4. **finally cleanup 동작성**: `upload_external_to_youtube` 내부의 `temp_dir_to_cleanup` 정리 구문이 누락 없이 그대로 이식되었음을 확인 (PASS).
5. **회귀 테스트**: `uv run pytest tests` 실행 결과 정상 구동 (PASS).

---

## 4. 남은 기술부채
- `main.py`에 독립적으로 유지된 `POST /api/projects/{project_id}/admin-publish-request` API는 다음 작업 단위인 `AIR-0144`에서 추가 조사 후 분리를 진행할 예정입니다.
