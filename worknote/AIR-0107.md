# AIR-0107

## Task ID
`AIR-0107`

## Date
`2026-06-28`

## Goal
- Validate the Longform image-page upload pipeline used for externally generated images and video clips.
- Apply the minimum safety fixes needed to prevent ambiguous or destructive bulk scene assignment.

## Context
- AIR-0106 established that AIR Studio creates prompts and manages assets while external services create images, upscale images, and generate clips.
- Bulk upload previously sent every asset to Gemini and silently allowed later files to overwrite the same scene slot.

## Files Changed
- `app/routers/image.py`
- `services/scene_asset_matcher.py`
- `templates/pages/image_gen.html`
- `tests/test_scene_asset_matcher.py`
- `docs/UPLOAD_PIPELINE.md`
- `docs/LONGFORM_PRODUCTION_PIPELINE.md`
- `project_status/WORK_INDEX.md`
- `project_status/LATEST.md`
- `project_status/NEXT_TASK.md`
- `project_status/ROADMAP.md`
- `worknote/AIR-0107.md`
- `worknote/latest.md`

## Tests / Validation
- `venv\Scripts\python.exe -m py_compile services\scene_asset_matcher.py app\routers\image.py tests\test_scene_asset_matcher.py`
- `venv\Scripts\python.exe -m pytest tests\test_scene_asset_matcher.py -q`
- Result: 5 passed, including a route-level deterministic upload check.
- Static trace covered scene slots, crop output, single upload, bulk upload, filename matching, Gemini fallback, ordering, invalid files, duplicates, missing scenes, and progress UI.

## Result
- Scene tokens in filenames are authoritative.
- Unnumbered files retain Gemini Vision fallback.
- Out-of-range scenes, occupied slots, and duplicate targets are not assigned.
- Bulk import returns explicit matched, unmatched, duplicate, invalid, and missing-scene data.
- `/image-gen` shows a result summary after import.
- Scene order continues to come from `ORDER BY scene_number`.

## Known Issues
- 2x2 crop output still downloads to the user's device and is not connected to project scenes.
- Valid unmatched files may remain stored without a scene link.
- Single-scene replacement has no version history.
- Upload progress is not byte-level or per-file.
- Browser-level mixed-media verification on the primary 8001 runtime remains needed.

## Next Action
- AIR-0108: connect 2x2 crop output to project scene ownership and add a manual reassignment path for unmatched stored files.

## Commit Hash
`9054841e`
