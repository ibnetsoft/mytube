# AIR-0115

## Task ID
`AIR-0115`

## Date
`2026-07-01`

## Goal
- Correct the local git remote so AIR Studio points at `ibnetsoft/mytube`.
- Re-check PR #11 and commit `f983dcf7` against the correct repository.
- Reorganize the AIR-0114 recovery notes under the Task=Branch=PR rule without adding product features.
- Keep `air-0109-scene-upload-pipeline` unchanged by moving cleanup work to `air-0115-git-remote-branch-cleanup`.

## Remote / Branch Cleanup
- Before cleanup, local `origin` pointed to `https://github.com/ibnetsoft/ilddang.git`.
- Correct repository confirmed by user: `https://github.com/ibnetsoft/mytube.git`.
- Updated local `origin` to `https://github.com/ibnetsoft/mytube.git`.
- Fetched `origin` from mytube.
- `git pull origin main` on the old local `main` produced conflicts because the local main had diverged from mytube `origin/main`; the merge was aborted.
- Created the AIR-0115 branch directly from `origin/main`:
  - `air-0115-git-remote-branch-cleanup`

## GitHub / PR Check
- GitHub CLI is authenticated as `ibnetsoft`.
- PR #11 exists in `ibnetsoft/mytube`.
- PR #11 URL: `https://github.com/ibnetsoft/mytube/pull/11`.
- PR #11 state: `OPEN`.
- PR #11 base: `main`.
- PR #11 head: `air-0112-longform-e2e-fix-pass`.
- PR #11 commits:
  - `9b4725e1` — `AIR-0112 enforce Longform asset readiness`
  - `f983dcf7` — `AIR-0112 finalize readiness record`

## ac78dd01 Check
- `ac78dd01` belonged to the previously configured `ibnetsoft/ilddang` remote / local branch history.
- After switching to `ibnetsoft/mytube`, visible `air-0109-scene-upload-pipeline` points to `36ac3364`, not `ac78dd01`.
- `ac78dd01` should not be treated as the mytube PR #11 commit.

## AIR-0114 Cleanup Result
- AIR-0114 local recovery notes were created while `origin` pointed at the wrong repository.
- The AIR-0114 conclusion that PR #11 / `f983dcf7` was unavailable was caused by repository mismatch, not by a broken mytube PR.
- The wrong-branch placeholder versions of `worknote/AIR-0111.md` and `worknote/AIR-0112.md` were not moved into AIR-0115.
- Correct state:
  - `worknote/AIR-0111.md` already exists on mytube `origin/main`.
  - `worknote/AIR-0112.md` exists on PR #11 branch `origin/air-0112-longform-e2e-fix-pass`.
- AIR-0115 carries only documentation about the remote/branch/PR cleanup, not Longform feature code.

## Scope Exclusion
Excluded from AIR-0115:
- Longform feature implementation.
- Shorts-related changes.
- Payout rebalancer changes.
- Admin demand API changes.
- DB migrations unrelated to remote/branch cleanup.
- Local workspace files and `.claude` changes.

## Validation
- `gh pr view 11 --repo ibnetsoft/mytube --json ...` succeeded.
- `git ls-remote https://github.com/ibnetsoft/mytube.git ...` confirmed mytube branches, including:
  - `main` at `59dca257`
  - `air-0112-longform-e2e-fix-pass` at `f983dcf7`
- PR #11 changed files are AIR-0112 Longform readiness files, not AIR-0115 remote cleanup files.

## Next Action
- Open a small AIR-0115 PR containing only this cleanup documentation.
- Keep PR #11 open for review; do not merge PR #11 or main from this task.
