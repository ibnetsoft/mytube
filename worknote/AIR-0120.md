# AIR-0120

## Task ID
`AIR-0120`

## Date
`2026-07-02`

## Goal
Remove the full-page reload triggered by worker-facing language switching.
Language icon clicks were causing a complete page reload plus on-demand recommendation translation through the Gemini → Claude → Google fallback chain, adding unnecessary latency to every language switch.

## PR
`#23` — AIR-0120 remove worker language full reload (MERGED 2026-07-02)

## Commit
`2046b53` (merge), `af593f5` (feature commit)

## Changes Made
- `services/i18n.py` — language preference persistence update; removed full-reload trigger path
- `templates/base.html` — language switcher UI updated to avoid triggering full-page reload on language icon click

## Scope
- Worker-facing UI change only
- No API or database changes
- No Admin UI changes
- No Supabase schema changes

## Note on Previous Worknote Content
An earlier draft of this file incorrectly described AIR-0120 as a PR #13 documentation-only cleanup task.
That description was stale and did not reflect the actual merged work.
The correct task is the language-switch reload removal above, as confirmed by git log (`2046b53`, `af593f5`) and PR #23.

## Next Action
AIR-0121 added an image workflow guide panel to `image_gen.html` to support workers with external AI image generation steps.
