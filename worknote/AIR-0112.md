# AIR-0112

## Task ID
`AIR-0112`

## Date
`2026-07-01`

## Goal
- Fix the highest-priority Longform failures identified by AIR-0111 without
  expanding product scope.
- Make Scene asset readiness a backend-owned contract shared by upload, review,
  project restoration, status polling, and render entry.

## Failure Before Fix
- Project 195 was `rendered` although only 2 of 11 Scenes had a visual asset.
- Scene Review calculated readiness only in browser JavaScript.
- Render did not enforce Scene asset completeness.
- Missing Scene-number gaps and duplicate Scene rows had no canonical status.

## Files Changed
- `services/longform_asset_readiness.py`
- `database.py`
- `app/routers/image.py`
- `app/routers/video.py`
- `main.py`
- `templates/pages/image_gen.html`
- `tests/test_longform_asset_readiness.py`
- `tests/test_scene_asset_review_ui.py`
- `docs/LONGFORM_USER_FLOW.md`
- `docs/LONGFORM_PRODUCTION_PIPELINE.md`
- `docs/SCENE_ASSET_PIPELINE.md`
- `docs/LONGFORM_MVP_VALIDATION.md`
- `project_status/WORK_INDEX.md`
- `project_status/LATEST.md`
- `project_status/NEXT_TASK.md`
- `project_status/ROADMAP.md`
- `worknote/AIR-0112.md`
- `worknote/latest.md`

## Tests / Validation
- Python compile passed for the readiness service and affected routers.
- Focused Longform suite: `34 passed, 1 warning`.
- Full suite: `47 passed, 1 failed, 2 warnings`; the remaining failure is the
  pre-existing async subtitle test configuration recorded by AIR-0111.
- Tests cover mixed image/video readiness, Scene order, gaps, duplicates,
  persistence, deferred-mode exclusion, and HTTP 409 render blocking.
- Live server on port 8002:
  - health returned HTTP 200
  - project 195 returned 18% completion and missing Scene 3-11
  - `project_complete=false` despite historical `rendered` status
  - render request returned HTTP 409
- Browser reached login. Authenticated UI verification remains blocked without
  dedicated test credentials.

## Result
- Formalized MVP policy `image_or_video`.
- Persisted `assets_ready`, completion percentage, readiness details, ready
  timestamp, and `project_complete`.
- Upload, bulk matching, replacement, prompt restore, full project restore, and
  status APIs now share canonical readiness.
- Longform render is blocked until canonical readiness succeeds.
- Scene Review shows backend completion and missing Scene numbers.
- Deferred modes are excluded.

## Known Issues
- Authenticated browser validation still needs a dedicated test account.
- Admin publish/export needs validation against the canonical contract.
- Historical projects are refreshed when relevant APIs access them; no
  destructive bulk migration was run.
- Stored unmatched assets still need a manual assignment workflow.

## Next Action
- AIR-0113: browser-verify crop, upload, matching, refresh restoration,
  readiness, render blocking, and final Scene order with a safe test worker.

## Commit Hash
`pending`
