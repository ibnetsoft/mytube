# AIR-0119

## Task ID
`AIR-0119`

## Date
`2026-07-02`

## Goal
Centralize scattered Claude/Gemini provider selection logic into a single routing module so that all AI generation calls share one source of truth for provider detection and fallback.

## PR
`#19` — AIR-0119 centralize AI provider routing (MERGED 2026-07-02)

## Commit
`24d06f9`

## Changes Made

### New file
- `services/ai_router.py`
  - `detect_provider(model)` — returns `'claude'` or `'gemini'` based on model name prefix
  - `generate_text(...)` — routes to `claude_service` or `gemini_service`; Gemini fallback on Claude failure

### Modified files
- `services/autopilot_service.py` — `generate_text_with_model` delegates to `ai_router.generate_text`
- `services/gemini_service.py` — title and image prompt generation use `ai_router.generate_text` via local import (avoids circular dependency)
- `app/routers/projects.py` — translation routing replaced with `ai_router.generate_text`
- `app/routers/user_topics.py` — provider chain selection uses `ai_router.detect_provider`

## Scope
- No Admin UI changes
- No Supabase or config schema changes
- No `*_MODEL` config key changes
- No functional behavior changes — routing logic only
- Fallback behavior preserved: Claude failure → `gemini-2.5-flash`

## Validation
- `python -m py_compile` passed for all 5 changed files plus `ai_router.py`
- No circular imports confirmed

## Backward Compatibility
- All existing `*_MODEL` config keys work unchanged
- Claude model names auto-route to Claude; all others to Gemini
- Gemini fallback on Claude failure preserved

## Next Action
AIR-0120 removed the worker-facing full-page reload triggered by language switching.
