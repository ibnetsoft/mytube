# AIR-0121: Image Workflow Guide

## Task Information
- **Status:** Done
- **Branch:** `air-0121-image-workflow-guide`
- **Commit SHA:** `8d6cb782` (feature branch tip) / `381fc97` (merge commit on main)
- **PR:** #25 (Merged to main 2026-07-03)
- **Completed:** 2026-07-03

## Goal
Add an image workflow guide panel to `image_gen.html` to help workers understand the external AI image generation pipeline (generation → upscale → crop → import), reducing onboarding friction.

## Key Changes
1. **Collapsible Workflow UI**: Added a UI container in `image_gen.html` that toggles between expanded and collapsed states, showing 7 steps from Prompt Generation to Ready for Video Generation.
2. **State Persistence & Auto-check**:
   - State is persisted to `localStorage` under `imageWorkflow_{projectId}`.
   - Step 1 (Prompt Gen), Step 6 (Upload), and Step 7 (Render) are automatically checked based on backend API callbacks.
   - Steps 2-5 are manually checkable by users.
3. **i18n Support**: Supported translations for ko, en, vi, th for step descriptions, external badges, and OpenAI ChatGPT usage hints (`services/i18n.py`).
4. **UX Improvements**:
   - Added `Prompt 복사` and `Open ChatGPT` quick action buttons directly under Step 2 to easily copy prompts and jump to ChatGPT.
   - External tasks explicitly labeled with `[External]` / `[외부 작업]`.
   - Minor related i18n key additions in `templates/pages/settings.html`.

## Commit History (within PR #25 branch)
| Commit | Message |
|--------|---------|
| `0581262` | AIR-0121 add image workflow guide |
| `81ad448` | AIR-0121 refine image workflow guide |
| `09ac148` | AIR-0121 refine image workflow guide final UX touches |
| `446a09a` | AIR-0121 add external badges and chatgpt hint |
| `8d6cb78` | AIR-0121 i18n external badges and list format for chatgpt hint |

## Verification
- Confirmed `GET /image-gen` and `GET /image-crop` endpoints return HTTP 200 via `test_endpoints.py`.
- No DB structure changes; purely frontend DOM and JS logic.
- i18n variables are correctly injected and translated into 4 supported languages.

## Note
PR #24 (Add Thai i18n keys for settings.html — Upload QA, Withdrawal, Referral) was merged on the same date (2026-07-03, commit `5c64f4d`), immediately before PR #25. See Non-AIR entry in WORK_INDEX for details.

## Scope
- Worker-facing UI addition only
- No API or database changes
- No Admin UI functional changes

## Next Action
Project status documents (WORK_INDEX, LATEST, NEXT_TASK, worknote/latest) were not updated after AIR-0119, AIR-0120, AIR-0121 merged.
AIR-0126 synchronizes all project status documents to reflect the actual GitHub main state.
