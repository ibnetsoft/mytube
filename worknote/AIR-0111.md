# AIR-0111

## Task ID
`AIR-0111`

## Date
`2026-07-01`

## Goal
- Validate the complete Longform Mode production journey from login through
  project completion from a CTO/MVP perspective.
- Identify Beta blockers without adding broad new functionality.

## Context
- AIR-0110A integrated AIR-0106 through AIR-0109 into `main`.
- AIR Studio owns planning, scripts, Scenes, prompts, import, matching, and
  review; users create images and clips in external AI services.

## Files Changed
- `docs/LONGFORM_MVP_VALIDATION.md`
- `project_status/WORK_INDEX.md`
- `project_status/LATEST.md`
- `project_status/NEXT_TASK.md`
- `project_status/ROADMAP.md`
- `worknote/AIR-0111.md`
- `worknote/latest.md`

## Tests / Validation
- Confirmed PR #9 was merged to `main` at `f5905d07`.
- Confirmed latest `origin/main` in a clean worktree.
- Started the integrated runtime and confirmed `/api/health` returns HTTP 200.
- Browser reached the Longform login screen.
- Authenticated browser flow could not continue without a supplied test
  password; no saved credentials were reused.
- Focused tests: `29 passed, 1 warning`.
- Full tests: `42 passed, 1 failed, 2 warnings`.
- The full-suite failure is missing async pytest support for the external
  subtitle generation script.
- Inspected actual SQLite project, settings, and Scene asset state.

## Result
- Longform is not ready for external Beta.
- The individual claim, plan, Scene import, matching, crop, and review pieces
  substantially exist.
- The primary blocker is the absence of one backend-owned `assets_ready` and
  `project_complete` contract.
- Existing persisted data can report `rendered` while most Scene assets are
  absent, proving terminal project status and the new asset workflow diverge.

## Known Issues
- No deterministic authenticated browser E2E fixture.
- No canonical Scene readiness policy.
- External-asset workflow and legacy internal generation/render UI coexist.
- Large video imports and unmatched asset recovery remain incomplete.
- Full pytest is not green because the async subtitle script is not configured
  as a deterministic pytest test.

## Next Action
- AIR-0112: define, persist, and enforce canonical Longform asset readiness and
  project completion across Scene Review, render/export, and project cards.

## Commit Hash
`pending`

