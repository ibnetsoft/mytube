# AIR-0131 — i18n Architecture Cleanup

Date: 2026-07-05
Branch: feature/AIR-0131-i18n-cleanup
Status: COMPLETE (pending PR creation)

## Summary

Full i18n cleanup pass across the worker UI.

## Changes

### P0-1: Key name mismatches fixed (base.html)
- Renamed 4 keys in `window.i18n` block: `btn_start_production→start_production`, `btn_start_batch→start_batch`, `btn_clear_logs→clear_logs`, `btn_go_to_results→go_to_results`
- Added 5 new keys to all 4 language sections: `status_start`, `status_analyzed`, `status_scripted`, `status_tts_done`, `btn_close_project`

### P0-2: current_lang conditionals removed
- `templates/pages/thumbnail.html`: 15 occurrences → 0 (8 in HTML, 7 in JS block)
  - Added 15 new keys to i18n.py (all 4 langs): `btn_more_clicky`, `btn_more_clean`, `thumb_reference_*` (5 keys), `history_*` (5 keys)
- `templates/pages/video_upload.html`: 8 occurrences → 0
  - Added 8 new keys to i18n.py (all 4 langs): `label_learning_notes`, `label_rating`, `placeholder_learning_note`, `btn_save_note`, `toast_note_required`, `toast_note_saved`, `toast_note_save_failed` + used existing `btn_refresh`
- `templates/pages/script_gen.html`: 1 text occurrence fixed (L148)
  - Added `tab_translation` key to all 4 langs
  - 2 other `current_lang` occurrences (L70/L73/L140) are structural DOM conditionals — kept

### P1: VI missing keys (116 keys)
- Added 116 missing VI keys to `services/i18n.py` VI section
- 57 keys had existing VI translations in the (buggy) TH section — reused
- 59 keys translated from EN to Vietnamese
- Key discovery: PLATFORM_TRANSLATIONS actually has 9 sections (ko/en/vi/es/th/id/fr/ru/pt) — not 4. Must use Python import to parse, not regex.

### QA Scripts
- `scripts/qa_i18n.py`: Audits i18n coverage + current_lang usage; uses Python import (accurate)
- `scripts/generate_th_translations.py`: Dry-run stub for TH translation generation

## i18n.py Architecture Note (CRITICAL)
PLATFORM_TRANSLATIONS has sections: ko, en, vi, es, th, id, fr, ru, pt
The `t()` fallback chain: current_lang → en → ko
Translator.data is set at init time; always use Translator('lang').t(key) for tests.
Do NOT use regex on the file to parse sections — use Python import.

## Test Results
- QA final: EN keys missing from VI = 0
- Server restart: API 200 OK
- Jinja2 template parse: thumbnail.html OK, script_gen.html OK

## Base.html residual current_lang (OK, keep)
Lines 75/80/85/90: CSS active-state for language flag buttons (structural)
Lines 137/138: `{% set %}` vars for initial server-render of nav labels (acceptable)
