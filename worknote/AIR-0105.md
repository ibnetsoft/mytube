# AIR-0105

## Task ID
`AIR-0105`

## Date
`2026-06-28`

## Goal
- Keep Gemini as the default Longform AI provider while adding Claude as a selectable provider for script planning, script generation, and image-prompt generation.

## Context
- AIR-0104 stabilized `/script-plan` access and removed the music-only planner UI from the standard longform worker flow.
- This task extends that flow with provider-aware AI routing so model/provider choice can be stored in project settings and reused on later generations.

## Files Changed
- `services/ai_provider.py`
- `services/gemini_service.py`
- `services/claude_service.py`
- `app/routers/gemini.py`
- `app/routers/user_topics.py`
- `main.py`
- `config.py`
- `templates/pages/script_plan.html`
- `auth-web/app/api/admin/settings/global/route.ts`
- `auth-web/components/DashboardContent.tsx`
- `tests/test_ai_provider_routing.py`

## Tests / Validation
- `venv\\Scripts\\python.exe -m py_compile services\\ai_provider.py services\\gemini_service.py services\\claude_service.py app\\routers\\gemini.py app\\routers\\user_topics.py main.py config.py tests\\test_ai_provider_routing.py`
- `venv\\Scripts\\python.exe -m pytest tests\\test_ai_provider_routing.py -q`
- `pnpm lint` in `auth-web` was attempted, but pnpm tried to run `install` and exited with `ERR_PNPM_IGNORED_BUILDS` in this environment.

## Result
- Added a provider-selection helper that normalizes invalid provider values back to Gemini.
- Added project/global configuration paths for `script_generation_provider`, `script_generation_model`, and image-prompt provider selection.
- Updated the longform planner UI so the provider/model choice can be saved with the project and reused on future generation actions.

## Known Issues
- The new provider selector has not yet been browser-verified in a live longform session.
- The admin lint command could not complete because pnpm attempted to bootstrap dependencies and hit ignored-build restrictions in this workspace.
- The provider/model story still needs a final product decision for whether image-prompt generation should use the same saved model as script generation or keep its own separate default.

## Next Action
- Browser-verify the longform planner provider flow.
- Decide whether the image-prompt model should stay separate or reuse the script-generation model by default.
- Commit and open the matching PR.

## Commit Hash
`57eac2db`
