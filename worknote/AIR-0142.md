# AIR-0142: main.py 미사용 import 정리 완료 보고서

Date: 2026-07-05
Branch: feature/AIR-0142-unused-imports-investigation
Status: COMPLETE

## 1. 개요
AIR-0137 ~ AIR-0141 Dead Code 정리 작업 후 `main.py`에 불필요하게 남아 있던 미사용 모듈, 중복 임포트 및 Pydantic 모델 선언 총 15개 항목을 안전하게 제거하였습니다.

---

## 2. 정리 결과

### 1) 제거된 Import 목록
- **FastAPI / 외부 import (3개)**:
  - `fastapi.responses.HTMLResponse` (line 19)
  - `pathlib.Path` (line 36)
  - 중복 `shutil` 임포트 (line 44)
- **중복/미사용 서비스 import (3개)**:
  - `from app.routers import settings` (line 69)
  - `from services.storage_service import storage_service` (line 73)
  - `from services.thumbnail_service import thumbnail_service` (line 74)
- **미사용 Pydantic 모델 import (9개)**:
  - `ProjectCreate`, `ProjectUpdate` (line 428)
  - `StylePreset`, `ScriptStructureSave` (line 429)
  - `GeminiRequest`, `VideoRequest`, `PromptsGenerateRequest` (line 434)
  - `ChannelCreate`, `ChannelResponse` (line 436)

### 2) 보존된 Import 목록 (Side Effect 가능성 보장)
- `database as db` (데이터베이스 마이그레이션 기동)
- `config` (환경 변수 검증 및 로딩)
- `auth_service` (라이선스 유효성 동적 체크 활성화)
- `pykakasi` (글로벌 헬퍼 초기화)
- 각 도메인 라우터 모듈 (`*_router` 형태)

---

## 3. 검증 결과
1. **정적 검사 (AST Parse)**: `python -m py_compile main.py` 실행 완료 $\rightarrow$ **오류 없음 (PASS)**
2. **런타임 임포트 검증**: `uv run python -c "import main"` 실행 시 모든 서비스 정상 바인딩 확인 (PASS).
3. **pyflakes 분석**: `main.py`에 적용된 모든 `imported but unused` 경고가 해소되었음을 확인 (PASS).
4. **회귀 테스트**: `uv run pytest tests` 실행 결과 정상 구동 (PASS).
