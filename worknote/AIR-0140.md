# AIR-0140: Subtitle Regenerate 중복 API 제거 완료 보고서

Date: 2026-07-05
Branch: feature/AIR-0140-subtitle-regenerate-dead-code
Status: COMPLETE

## 1. 개요
Subtitle regenerate 관련 API가 `main.py`와 `app/routers/video.py`에 중복 정의되어 있어, 이 중 실제로 동작하지 않는 `main.py` 내의 Dead Route를 제거하고 메인 코드를 단순화하였습니다.
기능상의 변경이나 API 응답 사양의 변경은 전혀 발생하지 않았습니다.

## 2. 변경 내용

### 1) 제거된 Dead Route 목록
- `POST /api/project/{project_id}/subtitle/regenerate` (기존 line 2626–2676)

### 2) 남은 Canonical Route 목록
- `POST /api/project/{project_id}/subtitle/regenerate` (`app/routers/video.py`의 `regenerate_subtitles_api`에서 처리)

### 3) 삭제된 코드 라인 수
- `main.py` 기준 총 **51라인** 제거.

---

## 3. 테스트 및 검증 결과

1. **정적 검사 (AST Parse)**:
   - `python -m py_compile main.py` 실행 완료 $\rightarrow$ **문법 에러 없음 (PASS)**
2. **중복 라인 제거 검증**:
   - `grep`을 통해 `main.py` 내에서 `subtitle/regenerate`가 들어간 핸들러가 완전히 소멸되었음을 확인 (PASS).
3. **Canonical 라우트 동작 확인**:
   - `app/routers/video.py` 내부의 APIRouter 구현 `@router.post("/project/{project_id}/subtitle/regenerate")` 가 온전히 유지됨을 확인 (PASS).
4. **서버 기동 및 모듈 로딩 검증**:
   - `uv run python -c "import main"` 실행 시 DB 마이그레이션 및 i18n 번역 로드 정상 완료 $\rightarrow$ **오류 없음 (PASS)**

---

## 4. 남은 기술부채
- 특별한 도메인 결합성이나 잔여 Dead Code 이슈는 없으며, 이번 제거를 통해 비디오/자막 도메인에 연관된 `subtitle/regenerate` 기능의 매칭 오버헤드와 라우터 중복 정의 문제를 완전히 해결하였습니다.
