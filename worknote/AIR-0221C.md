# AIR-0221C: Referral Audit Action Hotfix

## Goal
Add `'sending'` to `referral_audit_logs.action`'s CHECK constraint before Stage 2 dual-write implementation starts, closing the schema gap surfaced in `AIR-0221_Stage2_DUAL_WRITE_PLAN.md` §5 (the audit-action vocabulary was missing a value matching `referral_withdrawals.status`'s `SENDING` state).

## Actions taken
- `migrations/air_0221c_referral_audit_action_sending.sql` — forward migration, `DROP CONSTRAINT` + `ADD CONSTRAINT ... NOT VALID` + `VALIDATE CONSTRAINT` on `referral_audit_logs_action_check`.
- `migrations/air_0221c_referral_audit_action_sending_rollback.sql` — rollback, restores the original Stage-1 constraint (without `sending`).
- `migrations/air_0221c_referral_audit_action_sending_CHECKLIST.md` — apply checklist with a single-paste self-verifying script.
- Scope held exactly to `referral_audit_logs.action` — no other column, table, Gen 2/3 object, or application code touched.

## Production apply

**Applied 2026-07-09**, via the checklist's single-paste script through the Supabase Dashboard SQL Editor (project `picadiri`, branch `main`/production). Result: "Success. No rows returned", no error — under the script's design (DO-block assertion + `RAISE EXCEPTION` on failure), a clean success confirms the embedded verification (constraint exists, is validated, and its definition contains `'sending'`) passed before `COMMIT`.

## Independent post-apply verification (not just the dashboard screenshot)

`pg_constraint` isn't reachable via the PostgREST REST API (confirmed during Stage 1), so the constraint text itself can't be read back directly the way Stage 1's new columns/tables could. Verified functionally instead, via the target table itself (no other table touched):

1. Inserted a test row with `action = 'sending'` → **`HTTP 201`**, succeeded. Confirms the new value is now accepted.
2. Deleted that exact row immediately by `id` → **`HTTP 200`**. Row count confirmed back to **0** afterward (`Content-Range: */0`) — no residual test data left behind.
3. Sanity check: inserted a row with a deliberately invalid `action = 'not_a_real_action'` → **`HTTP 400`, `23514 check_violation`**, naming `referral_audit_logs_action_check` explicitly. Confirms the constraint is still actively enforcing (not accidentally dropped/disabled by this migration) — it now accepts exactly one more value, not "any value."

## Status
- Migration applied and independently verified.
- `AIR-0221_Stage2_DUAL_WRITE_PLAN.md` §5's "schema gap" note is now resolved — updated to reflect this.
- No code changed. No Stage 2 implementation started. No other table touched. No Gen 2/3 touched.
