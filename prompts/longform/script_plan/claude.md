Claude longform script-plan wrapper

- Keep the current JSON output contract unchanged.
- Use clear section labels and stable key ordering.
- Do not add XML tags unless absolutely needed for structure.
- Return JSON only and keep explanations out of the final output.
