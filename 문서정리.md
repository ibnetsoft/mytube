# 문서정리

## 이번 수정 요약

- STD 유저가 준비된 주제를 선택하면 6가지 생성 재료를 프로젝트로 복사하도록 보강했다.
- 준비된 주제 판정에서 `ready`뿐 아니라 `fallback_ready` 상태도 인정한다.
- 이미지/영상 프롬프트 필드명이 조금 달라도 인식하도록 보강했다.
- YouTube 업로드용 메타데이터는 기존 DB 컬럼이 없을 때 `progress_payload.publish_metadata`에서 읽도록 보강했다.
- 생성 결과 확인 페이지에서 장면별 이미지/영상 프롬프트, 제목 생성 데이터, YouTube 메타데이터가 보이도록 정리했다.
- STD 유저앱 이미지 생성 페이지에서는 이미지 생성 설정 섹션을 숨겼다.

## 사용자 흐름

1. 유저가 주제를 선택한다.
2. 준비된 기획, 대본, 이미지 프롬프트, 영상 프롬프트, 제목/설명 메타데이터가 프로젝트로 복사된다.
3. 이미지 생성 페이지에서는 프롬프트 자동생성 버튼을 누르지 않아도 준비된 프롬프트가 표시된다.
4. TTS 페이지에서는 복사된 대본을 기준으로 씬 대본이 표시된다.

## 확인 사항

- `tests/test_prepared_topic_assets.py` 통과.
- 관련 Python 파일 `py_compile` 확인.
- `auth-web` 관련 API lint 확인.
- 로컬 유저앱 `http://127.0.0.1:8000/` 응답 확인.
- 워커 대시보드 `http://127.0.0.1:3002/` 응답 확인.

## 배포 메모

- 로컬 개발 서버에는 바로 반영된다.
- 설치판 유저앱에는 Windows 빌드 후 새 릴리즈 배포가 필요하다.
- `topics_queue.publish_metadata` DB 컬럼이 아직 없을 수 있으므로, 현재 코드는 `progress_payload.publish_metadata` fallback을 사용한다.
